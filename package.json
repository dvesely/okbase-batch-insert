{
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run bundle & npm run sass",
    "bundle": "node bundler.js",
    "bundle:watch": "node bundler.js --watch",
    "sass": "sass src/www/main.scss dist/main.css",
    "sass:watch": "sass src/www/main.scss dist/main.css --watch"
  },
  "devDependencies": {
    "esbuild": "^0.8.49",
    "sass": "^1.32.8",
    "yargs": "^16.2.0"
  }
}
